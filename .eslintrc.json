{
  "extends": "standard-with-typescript", // colocando a lib que instalamos que é baseada no standard
  "parserOptions": { //dizer para o eslint que iremos utilizar configurações do tslint
    "project": "./tsconfig.json"
  }
}